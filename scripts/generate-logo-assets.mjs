import { mkdirSync, writeFileSync } from "node:fs";
import { join } from "node:path";

const OUT_DIR = join(process.cwd(), "public", "brand");

const colors = {
  ink: "#1a1f33",
  cream: "#fbf8f1",
  terracotta: "#c66a3d",
};

const horizontalDarkOnCream = `<!-- Generated by npm run brand:assets. Do not edit by hand. -->
<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 360 96"
  role="img"
  aria-labelledby="title desc"
>
  <title id="title">Pratix</title>
  <desc id="desc">Logo orizzontale Pratix scuro su fondo panna.</desc>
  <rect width="360" height="96" rx="18" fill="${colors.cream}" />
  <g transform="translate(28 24)">
    <rect width="48" height="48" rx="12" fill="${colors.ink}" />
    <rect
      x="0.75"
      y="0.75"
      width="46.5"
      height="46.5"
      rx="11.25"
      fill="none"
      stroke="${colors.terracotta}"
      stroke-width="1.5"
      opacity="0.45"
    />
    <text
      x="24"
      y="33"
      text-anchor="middle"
      font-family="Georgia, 'Times New Roman', serif"
      font-size="26"
      font-weight="500"
      letter-spacing="-1"
      fill="${colors.cream}"
    >
      P
      <tspan fill="${colors.terracotta}" font-style="italic">x</tspan>
    </text>
  </g>
  <text
    x="96"
    y="59"
    font-family="'Inter Tight', Inter, Arial, sans-serif"
    font-size="38"
    font-weight="600"
    letter-spacing="-1.4"
    fill="${colors.ink}"
  >
    Prati
    <tspan fill="${colors.terracotta}">x</tspan>
  </text>
</svg>
`;

const monochrome = `<!-- Generated by npm run brand:assets. Do not edit by hand. -->
<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 320 80"
  role="img"
  aria-labelledby="title desc"
>
  <title id="title">Pratix</title>
  <desc id="desc">Logo orizzontale Pratix monocromatico.</desc>
  <g color="currentColor" fill="none">
    <g transform="translate(16 16)">
      <rect width="48" height="48" rx="12" stroke="currentColor" stroke-width="1.8" />
      <text
        x="24"
        y="33"
        text-anchor="middle"
        font-family="Georgia, 'Times New Roman', serif"
        font-size="26"
        font-weight="500"
        letter-spacing="-1"
        fill="currentColor"
      >
        Px
      </text>
    </g>
    <text
      x="80"
      y="51"
      font-family="'Inter Tight', Inter, Arial, sans-serif"
      font-size="34"
      font-weight="600"
      letter-spacing="-1.2"
      fill="currentColor"
    >
      Pratix
    </text>
  </g>
</svg>
`;

mkdirSync(OUT_DIR, { recursive: true });

writeFileSync(join(OUT_DIR, "pratix-logo-horizontal-dark-on-cream.svg"), horizontalDarkOnCream);
writeFileSync(join(OUT_DIR, "pratix-logo-monochrome.svg"), monochrome);

console.log("Generated public/brand logo assets.");
